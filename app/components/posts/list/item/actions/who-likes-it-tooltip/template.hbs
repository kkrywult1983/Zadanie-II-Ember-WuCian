{{#if @post.likes.length}}
  <EmberPopover @side='bottom'>
    {{#each @post.likes as |like|}}
      <LinkTo
        @route='home.user.show'
        @model={{like.user}}
      >{{like.user.username}}</LinkTo>
    {{/each}}
  </EmberPopover>
{{/if}}